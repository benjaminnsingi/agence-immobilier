<!--- Hero --->
<section class="hero">
    <div class="glide glide1" id="glide1">
        <div class="glide__track" data-glide-el="track">
            <ul class="glide__slides glide__hero">

                <li class="glide__slide">
                    <div class="banner">
                        <div class="banner-content">
                            <span>À la recherche des meilleurs endroits</span>
                            <h1>Trouvez la maison de vos rêves!</h1>
                            <div class="buttons-group">
                                <button>Trouvez maintenant</button>
                            </div>
                        </div>
                        <img src="{{ asset('images/banner_1.png') }}" class="special_01" alt="">
                    </div>
                </li>

                <li class="glide__slide">
                    <div class="banner banner1">
                        <div class="banner-content">
                            <span>À la recherche des meilleurs endroits</span>
                            <h1>Trouvez la maison de vos rêves!</h1>
                            <div class="buttons-group">
                                <button>Trouvez maintenant</button>
                            </div>
                        </div>
                        <img src="{{ asset('images/banner_2.png') }}" class="special_02" alt="">
                    </div>
                </li>

                <li class="glide__slide">
                    <div class="banner">
                        <div class="banner-content">
                            <span>À la recherche des meilleurs endroits</span>
                            <h1>Trouvez la maison de vos rêves!</h1>
                            <div class="buttons-group">
                                <button>Trouvez maintenant</button>
                            </div>
                        </div>
                        <img src="{{ asset('images/banner_3.png') }}" class="special_03" alt="">
                    </div>
                </li>

            </ul>
        </div>

        <!-- Arrows -->
        <div class="glide__arrows" data-glide-el="controls">
            <button class="glide__arrow glide__arrow--left" data-glide-dir="<">
                <i class="fas fa-arrow-left"></i>
            </button>
            <button class="glide__arrow glide__arrow--right" data-glide-dir=">">
                <i class="fas fa-arrow-right"></i>
            </button>
        </div>

    </div>
</section>

<section class="section rent">
    <div class="title">
        <h2>Biens immobiliers à louer</h2>
        <p>
            Recherchez plus de 2000 propriétés à louer auprès des meilleurs agents du pays.
        </p>
    </div>

    <div class="rent-center container">
        <div class="box">
            {% for property in properties %}
            <div class="top">
                <div class="overlay">
                    {% for picture in property.pictures %}
                    <a href="{{ path('property.show', {slug: property.slug,'id': property.id}) }}">
                        <img src="{{ asset('uploads/' ~ picture.name) }}" alt="{{ property.title }}">
                    </a>
                    {% else%}
                    <a href="">
                        <img src="{{ asset('pictures/empty.jpg') }}" alt="{{ property.title }}">
                    </a>
                    {% endfor %}
                </div>
                <div class="pos">
                    {% for category in property.categories %}
                    <a href="{{ path('property.show', {slug: property.slug,'id': property.id}) }}"><span>{{ category.name }}</span></a>
                    {% endfor %}
                    <span>Achetez</span>
                </div>
            </div>
            <div class="bottom">
                <p>{{ property.title}}</p>
                <div>
                    <span>{{ property.price | number_format(0, ',', ' ') }} €</span>
                    <span><i class="far fa-eye"></i></span>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</section>