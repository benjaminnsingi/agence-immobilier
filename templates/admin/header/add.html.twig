{% extends 'dashboard.html.twig' %}

{% block title %}New Header{% endblock %}

{% block content %}
    <div class="container">
        <h1>Create new Header</h1>

        {{ include('admin/header/_form.html.twig') }}
        <br>
        <a href="{{ path('header_index') }}" class="btn btn-primary">back to list</a>
    </div>
{% endblock %}
