<form method="post" action="{{ path('header_delete', {'id': header.id}) }}" style="display: inline-block" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ header.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
